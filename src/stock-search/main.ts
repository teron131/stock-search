/** Run the Stock Search CLI. */

import { main } from "./cli.js";

if (import.meta.url === `file://${process.argv[1]}`) {
	main().catch((error) => {
		console.error(error);
		process.exit(1);
	});
}
